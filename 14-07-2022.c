#include <stdio.h>
#include <stdlib.h>

int main(){

	int num, flag, i, vet[50], pos;
	for (i = 0; i < 50; i++)
		vet[pos] = rand()%100;
		
	for (pos = 0; pos < 50; pos++)
	
	for(i=1; i<50; i+=2)
	{
		printf("%d %d\n", i, i+1);
	}
}
	
